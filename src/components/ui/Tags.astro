---
type Props = {
    tags: string[];
    link:(tag:string)=>string;
};
const {tags,link}:Props=Astro.props
---
<div class="flex flex-wrap gap-2">
    {
        tags.map((tag) => (
            <a
                href={link(tag)}
                class="bg-lp-surface-sunken border border-lp-border-subtle rounded px-3 py-1.5 text-xs text-lp-text font-sans hover:border-lp-border transition-colors"
            >
                #{tag}
            </a>
        ))
    }
</div>
